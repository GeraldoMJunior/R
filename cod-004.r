# Learning R Language
# Vetores: Objeto de dados básico
# Vetores com múltiplos elementos
# -------------------------------

v1 <- 1:10
v2 <- seq(5, 9, by = 0.5)
v3 <- c("Seg", "Ter", 'Qua')

cat(v1)
cat("\n")
cat(v2)
cat("\n")
cat(v3)